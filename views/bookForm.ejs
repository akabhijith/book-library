<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Book</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1><%= book ? 'Edit Book':'Add Book' %></h1>
    <form action="<%=book ? '/books/' + book.id : '/books' %>" method="POST">
        <input type="text" name="title" placeholder="Title" value="<%= book ? book.title : '' %>" required>
        <input type="text" name="author" placeholder="Author" value="<%= book ? book.author : '' %>">
        <input type="text" name="isbn" placeholder="ISBN (for cover)" value="<%= book ? book.isbn : '' %>">
        <input type="number" name="rating" placeholder="Rating (1-5)" value="<%= book ? book.rating : '' %>">
        <textarea name="review" placeholder="Review"><%= book ? book.review : '' %></textarea>
        <button type="submit"><%= book ? 'Update Book' : 'Add Book' %></button>

    </form>
</body>
</html>